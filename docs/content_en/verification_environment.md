# 1.1 验证环境

UVE 为以下工作提供统一的项目基础：

- IP 和模块级验证；
- 子系统及 SoC 集成验证；
- 可复用的 UVM 环境和验证组件；
- 基于 SystemVerilog、Python、cocotb 和 SVUnit 的测试；
- 仿真、回归、覆盖率、调试和结果分析；以及
- 早期架构与行为模型探索。

该环境既支持日常验证工作，也支持多个设计之间长期复用项目资产。
