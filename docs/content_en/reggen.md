# 寄存器生成器

寄存器生成器集成于 `uve_tools`，可从寄存器描述文件生成相关代码和文档。

## 支持的输入格式

- YAML / JSON / TOML
- XLSX（Excel）
- SystemRDL
- IP-XACT

## 支持的输出格式

- UVM RAL 模型
- SystemVerilog RTL
- C Header
- Markdown

## 使用方式

该工具以独立模块形式提供于 `uve_tools` 中。

详细的 CLI 选项和输入文件格式请参考工具自带文档。
