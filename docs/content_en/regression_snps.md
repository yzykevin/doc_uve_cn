# VCS 回归选项

## -s_regress2

使用 VCS 2-step 方法对提供的 testlist 进行回归测试。

## -s_regress3

使用 VCS 3-step 方法对提供的 testlist 进行回归测试。

## -s_rerun2_for

重新运行提供的回归文件夹中打开波形的失败测试（VCS 2-step）。

## -s_rerun3_for

重新运行提供的回归文件夹中打开波形的失败测试（VCS 3-step）。
