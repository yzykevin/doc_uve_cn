# 2.6 回归和覆盖率

回归能力包括：

- 基于 testlist 的执行；
- 并行用例调度；
- VCS 和 Xcelium 回归变体；
- 失败用例收集和重跑；
- 开启波形的调试重跑；
- 通过/失败汇总；
- 结果检查分析；
- HTML 报告；
- 日志和用例导航；以及
- 面向磁盘空间的已完成结果清理。

在所选仿真器和项目配置支持的范围内，覆盖率能力包括常见的行、条件、翻转、FSM、分支、断言和功能覆盖率工具流。
