# Xcelium 回归选项

## -c_regress1

使用 XRUN 1-step 方法对提供的 testlist 进行回归测试。

## -c_regress3

使用 XRUN 3-step 方法对提供的 testlist 进行回归测试。

## -c_rerun1_for

重新运行提供的回归文件夹中打开波形的失败测试（XRUN 1-step）。

## -c_rerun3_for

重新运行提供的回归文件夹中打开波形的失败测试（XRUN 3-step）。
